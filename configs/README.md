# Custom Dataset Configs

Built-in dataset defaults live in `dataset_plugins/` next to the dataset builders.

Use this directory for local JSON configs passed with `python3 main.py --config path/to/config.json`.
